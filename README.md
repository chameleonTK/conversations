# The Conversations


### Set up
1. `uv venv --python 3.11`
2. `source .venv/bin/activate`
3. `uv pip install openai ollama pandas numpy`
4. Run `uv run --with jupyter jupyter lab` to access the notebooks